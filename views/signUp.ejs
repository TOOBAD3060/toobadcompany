<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
     <div class="container signer">
         <h2 class="header">Create Account</h2>

         <%- include("./partials/messages") -%>
         <div class="doesNotMatch"></div>
         <span class="validEmail"></span>
         
         <form action="/signUp" class="signUpForm" method="post">
            <div class="form-group-mobile">
                <input name="firstName" type="text" id="registeredFirstName"
                 placeholder="First name" required
                 value="<%= typeof firstName != 'undefined' ? firstName :'' %>">
            </div>

            <div class="form-group-mobile">
                <input name="lastName" type="text" id="registeredLastName"
                 placeholder="Last name" required
                 value="<%= typeof lastName != 'undefined' ? lastName :'' %>">
            </div>

             <div class="form-group-mobile">
                 <input name="email" class="signupEmail" type="email" id="registeredMail"
                  placeholder="Email" required
                  value="<%= typeof email != 'undefined' ? email :'' %>">
              <img class="emailValid js" src="/images/checked.png" width="20px">
        

             </div>
             
             <div class="form-group-mobile">
                 <input  name="firstPassword" type="password" id="firstTime" required
                 placeholder="Enter password of 6-8 characters long"
                 minlength="6" maxlength="8"
                 value="<%= typeof firstPassword != 'undefined' ? firstPassword :'' %>">
                 <img class="firstTime js" src="/images/show.png" width="20px">
             </div>
             <div class="form-group-mobile">
                 <input name="password" type="password" id="confirmedPassword"
                 placeholder="Confirm password" 
                 value="<%= typeof password != 'undefined' ? password :'' %>">
                 <img class="secondTime js" src="/images/show.png" width="20px">

             </div>
    

             <button type="submit"  id="next">Sign up</button>
         </form>
         <span class="already">Already have an account?
              <a href="/login" class="hrefLinks">
                  Login
              </a>
              <a href="/verify">Verify</a>
              <a href="/verificationSuccess">Verify</a>
        </span>
     </div>



     <script src="/js/signUp.js"></script>
</body>
</html>